# YOLO11-S Optimization Benchmark

The baseline and optimized benchmark scripts use a common Jetson runtime measurement workflow so that TensorRT and pruned variants can be compared under consistent conditions.

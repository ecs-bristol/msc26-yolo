# TensorRT Optimization

This directory contains Jetson-local export utilities for the YOLO11-S FP16 and INT8 TensorRT variants.

INT8 export requires representative calibration data defined by `../../configs/int8_calibration.yaml`.
